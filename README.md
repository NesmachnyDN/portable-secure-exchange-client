# Portable Secure Exchange Client

Independent portfolio implementation of a cross-platform local-web client for controlled file exchange. The full implementation is being prepared on a feature branch before merge to `main`.
